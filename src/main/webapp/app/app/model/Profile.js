Ext.define('cwlover.model.Profile', {
    extend: 'Ext.data.Model',

    config: {
        fields: [
            'lable',
            'text',
            'disclosure',
            'xtype'
        ]
    }
});
